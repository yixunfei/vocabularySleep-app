# 记录 024：备份后“下一步 1/2/3”落地

## 时间
- 2026-04-13

## 背景
已完成 `PLAN_024` 备份式提交（`61c1265`）后，继续执行“下一步 1/2/3”：
1. Riverpod 迁移首批页面；
2. `DatabaseService` 继续拆分到分域仓库（settings/focus/ambient）；
3. 扩展模块关闭后的彻底停用语义。

## 动作
1. Riverpod 迁移与启动注入链路
   - 新增 `lib/src/state/app_state_provider.dart`。
   - `lib/src/app/app_bootstrap.dart` 在 `ProviderScope` 中增加 `overrides` 注入。
   - `lib/src/app/app_dependencies.dart` 改为单例 `AppState` 双栈注入：
     - Riverpod: `appStateProvider` / `cstCloudResourceCacheProvider` override；
     - provider: `ChangeNotifierProvider.value(value: appState)`。
   - 页面首批迁移：
     - `AppShell` -> `ConsumerStatefulWidget`
     - `SettingsHomePage` -> `ConsumerWidget`
     - `PracticePage` -> `ConsumerWidget`
2. 仓库分层继续拆分
   - 新增：
     - `lib/src/repositories/settings_store_repository.dart`
     - `lib/src/repositories/focus_repository.dart`
     - `lib/src/repositories/ambient_repository.dart`
   - `lib/src/services/settings_service.dart` 调整为仓库优先构造：`SettingsService.fromRepository(...)`。
   - `lib/src/services/focus_service.dart` 内部设置、番茄记录、待办、便签、排序等读写改为 `FocusRepository`。
   - `lib/src/state/app_state.dart` 下载环境音持久化路径改为 `AmbientRepository`。
3. 停用语义扩展
   - `lib/src/ui/pages/study_page.dart` 增加模块关闭直达守卫。
   - `lib/src/state/app_state_startup.dart` 增加 `study` 关闭时的运行时停止联动（`stop()`）。
4. 测试与适配
   - `test/ui_smoke_test.dart` 增加 `ProviderScope` 包装与 `appStateProvider` override，保证迁移期 smoke 用例稳定。

## 结果
- 已形成“Riverpod 读取 + provider 兼容注入”的迁移过渡层，后续可以按页面批量迁移而不必大爆炸切换。
- `settings/focus/ambient` 三个域完成仓库边界抽离，单体数据库在服务层的耦合面进一步收窄。
- 模块关闭语义从入口隐藏继续推进到学习域运行时行为阻断。

## 验证
- `./scripts/verify-local-analysis.ps1 -Task analyze -Target lib test` 通过。
- `flutter test` 全量通过。
