# 计划 450：全项目检查、缺陷修复与遗留闭环

## 基本信息
- **创建日期**：2026-09-23
- **状态**：已完成本轮确认范围；独立后续项已列明
- **负责人**：Codex
- **代码版本**：1.0.1+2
- **范围确认**：用户确认全项目扫描，优先可复现 bug 与已有明确设计的未完成内容。

## 目标与步骤
1. 获取全量静态分析、测试、i18n 与代码规模基线。
2. 检查学习/播放、睡眠、远程缓存与预热、工具箱异步流程及历史计划。
3. 以失败回归测试确认问题，再修复消除游戏跨局异步污染、睡眠播放器重复释放、资源预热取消/网络门控等明确缺陷。
4. 对其余确认的独立缺陷补充修复，避免无关格式化和机械拆分。
5. 完成全量回归，产出带证据与边界的质量评估，更新 changelog 后核对计划并提交本轮文件。

## 计划审查
- 保留既有发布平台与凭据策略；不改变 Android 签名、远程资源或数据格式。
- 不把 Web 受限入口扩展为完整产品；没有明确需求的功能不自行定义。
- 现有未跟踪词书处理脚本、数据和报告属于用户工作，不删除、不提交；分析结果区分这些文件。
- 改动通过新回归测试约束；新函数不超过 100 行，触达已有超限文件时按职责抽取必要部分。
- 不引入新依赖；现有 Flutter 测试框架、fake_async 和仓库接口足以验证。

## 验证方式
- `flutter analyze --no-pub` 与 `flutter test --no-pub --reporter expanded`。
- 定向失败复现/修复验证与触达文件格式检查。
- i18n audit、maintenance、旧 helper 与 Dart 插值扫描。
- 源码规模、历史计划和功能入口交叉检查；明确未进行真机验证的范围。

## 初始证据
- lib：623 个 Dart 文件，386,574 行；104 个超过 1,000 行，48 个超过 1,500 行，16 个超过 2,500 行。
- i18n：16,312 keys，占位符与参数错误为 0；3,505 个潜在未引用 key、417 条旧 registry source。
- 全项目分析：130 条诊断，含用户未跟踪 tool 脚本的诊断；待按类别统计。

## 执行结果
- 补齐消除游戏局次取消、睡眠播放器释放去重、预热取消/门控/预算重试/缓存完整性，以及 S3 编码、XML 与目录分页。
- 新增 17 个回归测试，强化既有释放断言并修正音量测试准备路径；新增测试先复现失败再修复。
- 消除游戏从 1,863 行拆为状态 536 行、消除流程 283 行、规则 565 行、展示 570 行；新增/调整方法不超过 100 行。
- 最终全量 `flutter test --no-pub --reporter expanded`：940 tests 通过；最后展示数值/S3 定向回归：17 tests 通过。
- 最终 `flutter analyze --no-pub`：0 error、4 warning（均来自用户既有未跟踪 tool 脚本）、124 info；lib/test 无 error/warning。
- 17 个触达 Dart 文件 `dart format --output=none --set-exit-if-changed`：0 changed。
- i18n 新增 key 0、退休 key 0；占位符审计全部错误项为 0。维护报告仍为 3,505 个潜在未引用 key、417 条旧来源、0 活动退休项，未扩大历史债务。
- 旧 helper 与 catalog Dart 插值扫描均无匹配；rg 不可执行，使用 Python/PowerShell 替代；没有恢复旧兼容 helper。
- 更新 changelog 后回看计划，补齐 PROJECT_DOMAIN 的实际技术栈与基础范围，并核对 PLAN_439/443/447 阶段状态。
- [质量评估](../docs/PROJECT_QUALITY_REVIEW_2026-09-23.md)记录后续边界：全量大文件治理、Web 完整实现、缓存 LRU/TTL、不同 validator 并发策略、压缩内容上限、释放超时与真机验收。
- 未进行 Android/Windows 发行构建、版本发布、远端写入、用户临时数据整理或历史凭据清理；用户既有工作不纳入提交。

