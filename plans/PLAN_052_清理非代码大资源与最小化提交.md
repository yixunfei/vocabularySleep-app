# 计划 052: 清理非代码大资源与最小化提交

## 基本信息
- **创建日期**: 2026-04-24
- **状态**: 已完成
- **负责人**: Codex

## 目标
清理与项目运行代码无关的大资源和示例工程，整理 `.gitignore`，并从本地未推送历史中移除已经被跟踪的非必要资源，使 `main` 可以正常推送到 GitHub。

## 详细步骤
1. 审计已跟踪的 ASR 模型包、第三方示例工程和当前 `.gitignore`。
2. 临时保护当前未提交工作区改动，避免清理历史时污染用户改动。
3. 从本地未推送历史中移除 `ASR/` 大模型包和 `third_party/flutter_tts/example/` 示例工程。
4. 更新 `.gitignore`，忽略 ASR 模型包、第三方示例工程、常见大模型/压缩包/导出物。
5. 更新 changelog，运行必要验证，创建最小清理提交并推送。

## 风险评估
- **风险 1**: 历史清理会重写本地未推送提交哈希。
- **缓解措施**: 仅处理尚未推送到远端的本地历史，并在操作前确认 `origin/main` 是当前分支祖先。
- **风险 2**: 忽略规则过宽可能误伤项目运行资产。
- **缓解措施**: 保留 `assets/branding/`、`assets/en_zh_15000_wordbook.json` 和 `assets/toolbox/` 等 pubspec 中声明的运行资产。
- **风险 3**: 当前工作区存在无关未提交改动。
- **缓解措施**: 操作前使用 stash 临时保护，历史清理后恢复。

## 依赖项
- GitHub 单文件 100MB 限制
- 当前远端 `origin/main`
