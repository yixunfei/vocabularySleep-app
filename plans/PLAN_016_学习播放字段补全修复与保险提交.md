# 计划 016: 学习播放字段补全修复与保险提交

## 基本信息
- **创建日期**: 2026-04-13
- **状态**: 已完成
- **负责人**: Codex

## 目标
1. 对当前工作区执行一次备份式保险提交，保留修复前安全点。
2. 修复学习播放仅播放单词本体、未播放已配置其他字段的问题。
3. 完成最小必要验证并同步更新 changelog。

## 详细步骤
1. 盘点当前工作区状态与学习播放相关入口，确认保险提交范围。
2. 创建计划文档，记录本轮修复目标、风险与验证路径。
3. 基于当前工作区创建一次保险提交，作为修复前回退点。
4. 定位学习播放的字段拼装与播放队列生成逻辑，修复已配置字段未进入播放序列的问题。
5. 补充或更新相关测试，验证单词本体与其他启用字段都能进入播放流程。
6. 更新 `changelogs/CHANGELOG.md`，回写本计划执行结果。

## 风险评估
- **风险 1**: 当前仓库已有大量未提交改动，直接修复可能与既有重构相互影响。
- **缓解措施**: 先执行保险提交，后续修复仅聚焦学习播放链路，避免扩大改动面。
- **风险 2**: 播放字段拼装可能同时影响学习播放、复习播放与导出配置。
- **缓解措施**: 优先复用现有字段配置模型，补充针对播放序列的验证，避免更改业务语义。
- **风险 3**: Flutter TTS 或第三方播放插件行为可能掩盖真正问题。
- **缓解措施**: 先检查应用内播放队列构建逻辑，再用测试验证进入 TTS 前的文本序列。

## 依赖项
- 当前分支 `codex/wordbook-core-refactor` 可正常提交。
- 学习播放相关代码与测试位于 `lib/src/services/`、`lib/src/state/` 与 `test/` 中。

## 执行结果
- 已创建保险提交 `68454a6`（`chore: backup before study playback field fix`），用于保留修复前安全点。
- 已修复播放配置中旧版字段禁用标记覆盖当前重复次数设置的问题，避免学习播放只播 `word`。
- 已补充回归测试，覆盖“重复次数开启但旧字段开关仍为禁用”时仍应继续播放释义的场景。
- 已验证 `flutter test test/playback_service_test.dart` 与 `flutter test test/database_service_test.dart` 通过。
