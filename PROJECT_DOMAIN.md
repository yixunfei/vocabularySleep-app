# 项目整体说明文档

## 文档版本
- **版本**: v0.0.53
- **更新日期**: 2026-05-28
- **状态**: 待完善

---

## 项目基本信息

| 项目属性 | 说明 |
|----------|------|
| 项目名称 | (待填写) |
| 项目类型 | Flutter 移动应用 |
| 目标平台 | iOS / Android |
| 最低版本 | (待填写) |
| 当前版本 | (待填写) |

---

## 项目背景与目标

### 项目背景
(待分析填写 - 请描述项目起源、核心需求来源)

### 核心目标
1. (待填写)
2. (待填写)
3. (待填写)

### 目标用户
- 主要用户群体: (待填写)
- 用户场景: (待填写)

---

## 技术栈

### 框架与语言
| 组件 | 版本 | 说明 |
|------|------|------|
| Flutter SDK | (待填写) | |
| Dart | (待填写) | |
| 状态管理 | (待选择) | flutter_bloc / riverpod / provider |
| 路由管理 | (待选择) | go_router / auto_route |
| 网络请求 | (待选择) | dio / http |

### 主要依赖
```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # 状态管理 (选择其一)
  flutter_bloc: ^8.1.3
  riverpod: ^2.4.9
  provider: ^6.1.1
  
  # 网络
  dio: ^5.4.0
  
  # 本地存储
  hive: ^2.2.3
  sqflite: ^2.3.0
  
  # 图片
  cached_network_image: ^3.3.0
  
  # UI组件
  flutter_slidable: ^3.0.1
```

---

## 项目结构

### 目录结构
```
lib/
├── main.dart                 # 应用入口
├── app.dart                  # 应用配置
├── core/                     # 核心模块
│   ├── constants/            # 常量定义
│   ├── theme/                # 主题配置
│   ├── utils/                # 工具类
│   └── extensions/           # 扩展方法
├── l10n/                     # 国际化资源
│   ├── app_en.arb
│   └── app_zh.arb
├── data/                     # 数据层
│   ├── models/               # 数据模型
│   ├── repositories/          # 数据仓库
│   ├── providers/            # 数据提供者
│   └── datasources/          # 数据源
│       ├── local/            # 本地数据源
│       └── remote/           # 远程数据源
├── domain/                   # 业务域
│   └── (按业务域组织)
├── presentation/             # 表现层
│   ├── pages/                # 页面
│   ├── widgets/              # 组件
│   └── blocs/                # 状态管理
└── services/                 # 服务层
    ├── navigation/           # 导航服务
    ├── storage/              # 存储服务
    └── network/              # 网络服务

scripts/
├── tooling-env.ps1                  # 共享工具链探测、PATH 注入与 CMake 缓存迁移清理
├── build.ps1                        # PowerShell 打包入口，支持 Windows/Android 等目标
├── dev-run.ps1                      # Windows 桌面快捷运行入口
├── test.ps1                         # Flutter 测试运行入口，统一 pub get、reporter 与缓存修复
├── verify-local-analysis.ps1         # 本地格式与 analyze 验证工具箱
├── opencode-minimax-m27.ps1          # 固定调用 MiniMax-M2.7 的命令模板
├── orchestrate-opencode-models.ps1   # 多模型 fan-out 调度脚本
└── opencode-model-profiles.json      # 外部模型 profile 映射配置
```

### 架构模式
- **状态管理**: (待选择: BLoC / Riverpod / Provider)
- **数据流**: (单向数据流)
- **依赖注入**: (待选择: get_it / riverpod / 手动)

### 开发辅助工具
- 当前仓库已内置本地验证脚本与 `opencode` 外部模型协作脚本，供开发期分析、方案草拟、并行对比和调度使用。
- `scripts/tooling-env.ps1` 集中处理 Flutter、Dart、CMake、Android SDK、NuGet 的解析与 PATH 注入，优先读取环境变量，再回退到项目内 `.fvm`、`.tooling` 和常见安装目录。
- `scripts/build.ps1`、`scripts/dev-run.ps1`、`scripts/test.ps1`、`scripts/verify-local-analysis.ps1` 会检查 `CMakeCache.txt` 是否残留旧工作区绝对路径；若发现从旧盘符复制带来的缓存错配，会清理对应生成目录后再继续。
- 运行时脚本不写死个人机器绝对路径；外部素材和生成数据默认使用环境变量、显式参数或项目内 `build/generated/` 输出目录。
- 外部模型输出仅作为辅助参考，不应直接替代对业务代码、架构边界和实际仓库上下文的本地判断。
- 多模型调度默认将结果写入工作区 `.tmp_model_runs/`，便于后续复查与归档。

### toolbox 设计文档
- `docs/toolbox_design/TOOLBOX_DESIGN_REVIEW.md`: 工具箱模块设计评审与问题清单。
- `docs/toolbox_design/TOOLBOX_ANIMATION_SPEC.md`: 工具箱动效规范与节奏建议。
- `docs/toolbox_design/TOOLBOX_UI_STYLE_GUIDE.md`: 当前生效的 toolbox UI 设计基线，用于约束后续“只动 UI、不动逻辑”的精修方向。
- `modules/toolbox/README.md`: 工具箱模块说明，包含每日抉择六模块基础版的数据边界、自定义存储和后续扩展路线。

---

## 功能模块

### 核心功能清单
| 模块 | 优先级 | 状态 | 备注 |
|------|--------|------|------|
| (待填写) | (待定) | 待开发 | |

### 功能特性矩阵
- 工具箱/每日抉择已从占位转盘升级为六模块基础版：
  - 吃什么、穿什么、去哪儿、干什么提供本地种子数据、随机选择、详情/指南和自定义管理；其中“吃什么”已支持个人食谱集、集合内筛选随机、内置菜个人调整与另存为个人食谱。
  - 其中“穿什么”已补充基于多本本地穿搭资料整理的规则库、`87` 条温度/场景搭配种子、天气体感驱动的默认档位建议、模块化穿搭指南，以及支持 `风格 / 版型 / 样式类型 / 面料 / 亮点` 的结构化衣橱管理。
  - 随机助手提供均匀、加权、联合分布多轮抽取，并配套大转盘、骰子和硬币动画；仅用于低风险、可回退的轻量选择。
  - 决策助手提供均匀随机、期望加权、因子评分和联合概率等透明计算工具。
  - 自定义项与吃什么个人食谱集暂使用每日抉择独立 JSON 文件存储，不侵入主数据库。
- 工具箱/人类测试新增独立 `toolbox.human_tests` 子模块：参考 Human Benchmark 类测试条目组织，提供反应测试、数字记忆、黑猩猩测试、打字测试、视觉记忆、瞄准测试、色觉测试、斯特鲁普、词汇记忆、序列记忆、运气测试、手速测试、时间感知测试、手眼协调测试、双手协调挑战、计算能力测试、动态视力测试和持续注意力测试；其中双手协调挑战已重做为左右独立脑裂小游戏集合，覆盖画图、弹球、楼梯、长按充能和同步窗口节奏，并补上手机默认 90 度横屏沉浸全屏、进入即开局、手眼协调同款全屏浮层、默认无限时长、轮数设置和窄屏左右并排；结果仅在本地页面即时展示，不进入主数据库或用户学习记录。
- 工具箱/声源定位新增独立 `toolbox.sound_locator` 子模块：默认使用手机自带麦克风和多位置移动确认，Flutter 端落地监听控制、空间指引舞台、移动采样点记录、下一步移动提示、多声源候选列表和回响/信噪比风险提示；双声道输入可补充 TDOA 水平方位估计，单声道输入也可通过多个位置的强度和质量差异逐步收敛声源区域，ODAS/阵列输出仅作为可选高级证据。
- 工具箱/加密安全新增独立 `toolbox.crypto_security` 子模块：入口为“加密安全中心”，使用独立子模块卡片承载后续加密、解密、密钥、证书、校验、签名等能力，不用页签作为总模块结构；当前先从生活实用迁入图片/音频/视频媒体隐写。媒体隐写支持文本/文件 payload 写入图片、WAV/PCM 音频和 MP4/MOV 视频载体，内置 AES/Twofish/Camellia/ChaCha20-Poly1305 固定与自由级联、256/512/1024-bit 每层密钥材料、Whirlpool MAC/哈希、弱签名版 SHA-256/HMAC 轻量标签、SHA-256/RSA 与 ECDSA 高成本完整性附加校验、口令 + keyfile 混合派生和随机 keyfile 导出；弱签名版使用用户派生签名种子、内置域 key 和随机 nonce/padding 生成轻量标签，不生成公私钥，适合低性能移动设备附加校验，但不提供第三方来源证明；RSA/ECDSA 签名密钥当前仍由口令域确定性派生，公钥也写入同一 envelope，因此只作为同一口令域内的完整性附加校验，不作为独立来源证明；图片 LSB 使用密钥派生头与 CSPRNG 随机像素顺序，WAV/PCM 使用密钥派生头与随机样本 LSB 顺序，MP4/MOV 使用容器内 `free` box 承载带长度掩码与随机 padding 的加密块，三类新写入均支持定位密钥算法与强度设置，新写入定位器默认 4096 轮起步、strong/extreme 为 12000/24000 轮；新隐写保护块在数据段前记录一字节最大错误尝试次数，尾部加入前置数据轻量格式校验，图片/音频写入基于载体内容派生位置的轻量策略头，视频写入基于前置容器内容校验的策略头，以便本机错误口令场景提示和清理兜底，但公开策略头、尾部校验和错误/成功次数限制均不作为抗恶意篡改、抗副本离线尝试或阅后即焚的安全边界，真正认证来自 crypto envelope 的 MAC/AEAD；由于模块尚未正式发布，新 crypto envelope 已升级并移除公开 `plainSha256` 与 `keyFileSha256`，keyfile 匹配交由 MAC/AEAD 失败处理，且不再接受 v2/v3 crypto envelope；文本/文件隐写生成与还原在 UI 层使用 `compute` isolate 执行，降低图片解码、LSB 定位、WAV 样本定位、MP4 box 解析、KDF、scrypt 和 AEAD 对主线程的阻塞；文件选择阶段已改为先检查 `PlatformFile.size`，再通过 read stream 或文件路径分段读取，避免 file_picker 在移动端 `withData: true` 预读整文件；触发篡改提示、达到载荷错误次数或成功还原次数上限时，仅对当前内存载体和可写源路径执行最佳努力清理；页面已将 keyfile 收口为开关式选项，支持多 keyfile 稳定排序组合，并强化高级设置折叠区辨识度，还原功能隐藏写入阶段才相关的加密算法、强度、签名和级联设置，仅保留口令/keyfile 与定位设置，并按当前文件统计 5 分钟内连续错误，达到 10 次后仅锁定该文件 5 分钟；RC4/SHA256 stream 仅保留旧载荷还原兼容，旧音视频裸尾部载荷仅保留还原与清理兼容，新音视频写入不再使用尾部裸追加；WAV/PCM 隐写不抗 MP3/AAC 等有损转码，MP4/MOV `free` box 隐写不抗会剥离自由 box 的重封装；该能力只处理本地媒体载荷，不进入主数据库或学习记录。
- 加密安全/媒体隐写写入流程已增加当前格式载体占用闸门：对可确认已由本模块写入的载体拒绝再次隐写，避免重复写入导致旧载荷残留或新旧定位冲突；如需多层内容，用户可显式开启文本/文件双层模式，图片使用分离像素槽位，WAV/PCM 使用分离随机样本槽位，MP4/MOV 使用成对受保护 `free` box，表层与深层使用不同口令，表层口令只还原表层内容，深层口令只还原深层内容。高级保护块 `VSSG3` 同时支持“成功还原次数上限”，到达次数后优先清理当前可写文件副本或当前内存载体；该限制不约束已复制文件、外部备份或换客户端读取。
- 加密安全/媒体隐写页面已将“写入/还原模式”和“媒体类型”拆成独立状态，隐写文本与隐写文件流程都可正常选择载体类型；自由级联组合项贴近加密算法展示。写入前会按当前算法、级联层数、MAC、签名模式和最大随机 padding 估算载荷封装尺寸，并结合图片 LSB、WAV/PCM 样本 LSB 或 MP4/MOV 容器可追加容量提前提示当前容量、需要预留容量、文件大小上限和最低建议载体；文本/文件写入均提供“预览写入”操作，生成前展示媒介、容量、预计载荷、预计输出和双层提示；估算偏保守，目的是避免实际写入阶段因随机封装尺寸波动失败。导出 keyfile、隐写媒体或文件结果时，若平台返回可写路径会主动写入字节，避免仅记录路径；解密文件导出完成、成功/错误次数触发清理时，页面会卸载当前载体字节与预览，减少已清理文件仍残留在内存中的风险。
- 工具箱/生活实用将原本分离的 `image_compress` 与 `image_upscale` 收口为统一 `image_transform` 子模块：页面内通过“压缩 / 扩大”双页签快速切换，压缩能力保持原有离线流程；扩大能力支持按倍率、按目标宽高和画布扩展补像素，并新增 `Fast duplicate` 低 CPU 放大算法与自定义倍率输入。透明/纯色扩边优先走 `image` 库现成画布扩展路径，减少手工逐像素填充的 CPU 压力；该能力仍仅做本地插值与画布处理，不是生成式 AI 超分辨率，也不承担隐写写入逻辑。
- 工具箱/生活实用新增 `ai_interview` AI 面试训练子模块：参考 `Zippland/Snap-Solver` 的题目输入、多模型偏好和提示词配置思路，但落地为本地练习与答案组织工作台，不实现屏幕捕获、后台监听或实时代答。页面支持输入面试题、目标岗位、公司/团队、简历亮点和回答草稿，并可配置题型、分析深度、输出语言、表达风格、模拟追问和草稿评分严格度；服务层在本地启发式识别行为面试、技术问答、系统设计、产品/业务 case 与 HR/动机题，输出回答框架、准备度评分、草稿体检、追问、提示词草稿和下一步清单。复制提示词到第三方 AI 前应移除敏感简历、薪资、客户资料、公司保密题和 NDA 内容。
- 工具箱/生活实用新增 `offer_select` Offer 选择助手子模块：参考 `https://offerselect.zippland.com/` 的个人履历、工作机会、月 Base、发薪月数、其他奖金、薪资/WLB 评级、福利、社保、工作强度、地点、优缺点和已婉拒输入形态，落地为本地多 Offer 决策工作台。服务层复用工作性价比口径，并按现金流、时间成本、健康边界、成长匹配、稳定性和城市适配六维计算综合分，输出首选、领先差距、风险旗标、优势说明和非首选追平首选所需的月 Base 加薪估算；页面保留 Offer 打分、城市对比和 AI 笔试/面试参考工具链入口。该模块不上传 Offer 数据，不替代职业、法律、税务或财务建议，合同主体、试用期、奖金兑现、社保缴纳和真实工资条仍需用户自行核验。
- 工具箱/生活实用新增 `image_to_web` 图片转网页子模块：选择本地图片后可生成单文件 HTML 图片预览页，HTML 内嵌 Base64 图片并支持标题、替代文本、完整显示/封面裁切/自然尺寸和浅色/深色/棋盘格背景；可复制 HTML 或保存 `.html` 文件，保存对话框不可用时回退到应用文档目录 `life_tools/image_to_web`。同页新增基于 Uguu 的临时图片分享：使用 `POST https://uguu.se/upload` 与 `files[]` 表单字段上传原图并返回公开临时 URL，可复制或打开；本地 HTML 生成不上传图片，Uguu 分享会把原图交给第三方临时文件服务，当前公开 FAQ 声明约 3 小时删除，但服务可达性、限制、策略与保留行为可能变化，用户不应上传证件、隐私截图或敏感图片。
- 工具箱/生活实用新增 `id_photo` 证件照生成子模块：参考 QQ 浏览器证件照网页的“选择照片、裁切、换底色、选择规格、保存”流程，落地为本地离线处理工具。页面支持选择本地头像照片，按 1 寸、小 1 寸、大 1 寸、2 寸、护照和 2x2 签证照等常见规格结合 DPI 生成目标像素图，提供裁切缩放、横纵位置、底色、PNG/JPEG 导出和结果预览；服务层使用 `image` 包完成方向烘焙、裁切、缩放、四角取样简易背景替换和编码。该模块不上传照片，不做人像数据持久化；简易换底色适合纯色或接近纯色背景，不等同 AI 抠图或机构级证照审核，复杂背景、发丝边缘、阴影和具体尺寸规范仍需以办事机构要求为准。
- 工具箱/生活实用补齐 `short_link` 与 `qr` 两个网络/编码工具：短链接支持 TinyURL、is.gd、v.gd、CleanURI 多服务生成、URL 规范化、自定义别名、本地离线短码和重定向链路还原；二维码支持文本、URL、Wi-Fi、联系人、邮件、短信、电话、地理位置、日历、小图 Data URL 等模板，本地生成 QR Code、Data Matrix、Aztec、PDF417，并提供容错等级、自动/V1-V40 精确版本、留白、颜色/圆角样式、中心图和 PNG 导出。图片 Data URL 导入会在 32MB 读取硬上限内自动多档压缩到约 2.2KB 的二维码友好目标，并在实际 QR 容量不合时继续回退到更小目标；页面展示原图体积、最终缩略图尺寸、编码体积和 Data URL 体积，若最小候选仍过大则提示改用中心图或继续降低最长边。二维码艺术化升级为 QArt 风格半调融合：导入图片后会先自动压缩为渲染友好的艺术图副本，再按半调画像或透明融合方式自绘二维码模块；渲染层根据图片亮度、边缘和稳定抖动计算模块墨量，让图像轮廓参与二维码主体；finder、timing、alignment、format/version 等功能区保持高对比，并提供可扫性保护层、图像对比强度、模块留白和弱纠错 V25 艺术预设。当前属于 Flutter 渲染层视觉融合，不是完整 QArt 编码位优化算法；弱纠错预设会降低污损/遮挡容忍度，正式使用前必须实机扫码。QR Code 预览会在渲染前强制进行容量校验，超出当前版本/容错可承载范围时只显示错误面板并禁用导出，不再把超长 payload 交给 `QrImageView` 触发框架异常。公开短链 API 的访问策略、额度和地域可达性可能变化，本地短码不是公网短链接；重定向链路还原只展示跳转过程，不审计目标安全性；图片 Data URL 二维码仍只适合很小的头像、图标或签名图，品牌展示优先使用中心图；艺术图压缩只影响预览渲染，不会把艺术图内容写入二维码；Wi-Fi、日历、联系人和非 QR 标准在不同扫码器中的兼容性需要实测。
- 工具箱/生活实用新增 `timeline_periodic` 历史年表/元素周期表可视化子模块：历史年表内置来自 NASA、USGS、ICS、Smithsonian、Britannica 等公开来源交叉整理的宏观共识节点，并用“约/范围”表达不确定年代；元素周期表内置 PubChem PUG REST 整理的 118 元素数据，并保留 IUPAC 与 CIAAW 等权威来源入口。页面提供时间范围/主题筛选、对数历史轴缩放拖动、元素族块/状态筛选、元素详情和来源跳转，不新增运行时网络依赖。
- `timeline_periodic` 历史年表默认展示已改为纵向故事轴，避免深时间节点在窄屏横向轴上互相遮挡；横向缩放轴保留为可选全景地图视图，并新增沉浸全屏模式。时间轴背景使用 NASA Image Library、USGS 公开媒体说明和 Wikimedia Commons 文物图片等远程公开资源，失败时回退为本地渐变，不影响核心数据阅读。
- `timeline_periodic` 人类史节点已扩展为“31 个核心深时间锚点 + 68 个基础人类史扩展节点 + 57 个前现代密集节点 + 46 个近现代密集节点”的组合体系，总计覆盖 202 个宏观节点；扩展部分按人类演化、史前定居、农业城市、古典世界、宗教思想、跨大陆交流、中世纪、早期现代、殖民革命、工业化、世界大战、冷战、去殖民化、全球治理、数字时代和当代科技组织。远程背景图加载改为带超时和缓存的受控请求，网络失败仅回退本地渐变，避免图片服务层 SocketException 影响页面。
- 工具箱/生活实用将原“文本编码”入口升级为 `text_encoding` 文本转换子模块，并把原独立“中文转拼音”能力并入同页：页面以分组切换统一承载拼音/注音、简繁转换、阿拉伯数字转罗马数字/中文数字/人民币大写、万年历与农历公历互转、干支/四柱八字、本地六十甲子查询、国家语言代码查询，以及 Base64、URL、HTML、Unicode、JSON、Morse、兽语、RC4 legacy、火星文、零宽字符文字隐藏、仿古竖排排版、常见乱码候选修复与常用码表查看；输入区按模式只展示必要字段，例如 RC4 才显示密钥、文字隐藏才显示隐藏内容、竖排才显示列数/行数字段。
- 文本转换中的农历、干支、八字、六十甲子与语言代码能力均以本地参考查询为定位：默认使用北京时间与北京真太阳时辅助计算，八字反查仅在受限年份范围内按时辰枚举候选，不作为权威命理、历法校勘或官方标准代码库替代。
- 工具箱/生活实用中的 `compass`、`level`、`vibration` 三个设备工具已重做为独立首屏舞台：指南针整合磁力计与加速度计做倾斜补偿和平滑指针，并显式展示朝向、持机倾角与磁场稳定度；水平仪提供 `Level / Plumb` 双模式、俯仰/横滚读数、气泡舞台和接近找平状态；震动仪提供节奏预设、脉冲宽度、间隔、循环次数与图形化预览，Android 端通过 `vocabulary_sleep/life_device` 原生通道优先走 `Vibrator/VibrationEffect` 波形能力。三者都以移动端快速判断为目标，不承诺工程级精度或所有平台都具备完整硬件支持。
- 工具箱/生活实用中的 `ruler` 尺子和量角器提供横屏边缘直尺、手动校准和相机背景量角器；量角器刻度层已增加半透明深色测量盘、深色描边、暖色主刻度和浅蓝灰次刻度，避免白纸、白墙、桌面等浅色相机背景与白色刻度混在一起。该模块仍是日常辅助测量工具，刻度精度受设备物理尺寸、屏幕缩放和手动校准影响，不替代专业量具。
- 工具箱/生活实用新增三项本地实用工具闭环：`device_frame` 带壳截图支持本地截图导入、原创前视机模合成、背景氛围、状态栏覆盖、炫光控制与 PNG 导出，并进一步扩展为五套原创前视机模、窄屏自适应预览与可定制时间/电量/网络状态栏；`unit_converter` 全能单位换算支持长度、重量、温度、面积、体积、速度、力、密度、功率、热量/能量、数据、时间、CSS 尺寸多类别和双向输入，并新增证件照尺寸参考模式，补齐光年、里、微米、纳米、市斤、微克、海里/小时等高频单位；`bmi` BMI 计算器已扩展为成人/儿童青少年身体指标工作台，支持公制/英制、年龄、性别、中国成人/WHO 成人分界、CDC 2-19 岁 BMI-for-age 百分位、健康体重区间、腰高比、腰臀比、Mifflin-St Jeor 基础代谢和活动水平总消耗估算。带壳截图当前重点提升真实展示感，但仍不对应任何厂商官方营销图或精确 OEM 尺寸；单位换算继续依赖本地静态单位表，BMI、围度和能量结果仅用于日常自查与趋势管理辅助，不替代医学评估。
- `device_frame` 带壳截图已进一步收口为“截图舞台 / 构图设置 / 状态栏设置 / 使用边界”的移动端工作流：首屏聚焦本地截图导入、带壳预览和 PNG 导出；构图区新增 `裁切填满 / 完整显示` 截图适配，并在原图比例与机模屏幕不一致时提示裁切风险；导入阶段非 Web 优先使用 read stream 或文件路径读取并限制 32MB 大图，降低移动端预读整文件压力。该模块仍只处理本地图片合成，不上传截图，不生成厂商官方营销素材。
- `device_frame` 状态栏模拟已增强为拟真展示：右侧状态组贴近屏幕外壳内侧并限制不超过屏幕宽度一半；电池按充电中、正常、低电量（<30%）和电量不足（<15%）切换图标与颜色，充电状态由用户显式设置。该状态栏只用于出图模拟，不读取真实设备电量或网络。
- 工具箱/生活实用新增 `city_compare` 城市薪资对比子模块：参考 `Zippland/city_compare` 的静态城市样本，在本地估算双城市同生活方式下的月成本、到手收入、月结余、目标城市保本薪资，以及“保持当前生活方式与月结余所需的目标税前月薪”。页面把住房、餐饮、交通、教育、水电网话、健身和娱乐拆开展示，并使用简化五险一金与个税模型辅助 offer/迁居横向比较；该工具不是实时房价、学费、税务或工资条模拟器。
- `city_compare` 当前还会保留参考页里的更多原始字段用于对照，包括社保基数上下限、不同租房档位、房价、便宜餐与中档餐、手机套餐、宽带、各教育阶段、健身和电影票等，并在页面内自动生成“最大差异项 / 同薪资迁移结果 / 原始参考条目提示 / 维持当前结余所需月薪”等分析结论，帮助用户理解“贵在哪里、便宜在哪里”。
- `city_compare` 现已支持个人校准层：可按住房、餐饮、交通、教育、水电网话、健身、娱乐等类目对预设基准做倍数微调，也可单独覆盖健身月费和电影票价，并把自定义月支出统一并入双城月成本。
- `city_compare` 的自定义支出适合补足宠物、照护、订阅、债务、康复、兴趣训练等静态样本难以标准化的开销；这些输入属于个人修正，不代表官方均价或实时市场口径。
- 工具箱/生活实用新增 `mortgage` 房贷计算器专业版：支持等额本息、等额本金，支持按贷款金额或住房面积、单价、首付比例/首付金额反推贷款本金，并结合贷款年限、年利率、首次还款时间、已还期数、提前还款本金、月度附加费用和一次性费用生成合同期还款明细与剩余还款计划。页面首屏展示下期月供、贷款金额、原始总利息、剩余本金和剩余时间，下方拆分合同总费用、已还本金/利息/费用、剩余利息、预计结清日期和后续 12 期明细。提前还款支持“月供降低”和“期限缩短”两种估算口径；结果仅用于本地测算和方案比较，不替代银行合同、审批结果、LPR 调整、组合贷规则或地区税费政策。
- 工具箱/生活实用中的 `notify_me` 通知自己已收口为提醒工作台：首屏展示当前提醒预览、提醒时间、实际触发时间、下一条提醒和投递能力；创建区支持状态栏通知/锁屏提醒、指定时间/倒计时、预设或自定义提前分钟、自定义倒计时、状态栏常驻、进入应用取消和系统日历镜像；列表支持完成、稍后 10 分钟、稍后 1 小时、套用配置和删除。Android 原生通知动作补齐完成、稍后、打开和忽略，但完整锁屏/精确触发体验仍受通知权限、精确闹钟权限和厂商后台策略影响。
- 工具箱/生活实用中的 `fake_call` 模拟来电已从待办/通知提醒/系统日历链路中剥离，重做为独立一次性模拟来电工具：页面首屏展示来电预览舞台，支持姓名、号码、归属地、标签、倒计时或指定时间、系统铃声和震动开关；点击开始后进入等待状态，到点播放全屏模拟来电，取消仅取消本次会话，不创建 `life_fake_call` 待办，也不写入系统日历。触发后的全屏来电支持拒绝、接听、通话计时和挂断流程，接听会停止来电铃声/震动并切换为通话效果，通话中以居中大面积脉冲动画舞台承载接听状态，挂断/拒绝会清理本次通知并在 Android 原生页最佳努力返回桌面；来电中与通话中可分别配置自定义背景图片和 Cover/Dim/Blur 样式。Android 端通过独立 `vocabulary_sleep/fake_call` 通道、`FakeCallScheduler`、`FakeCallReceiver`、闹钟级调度、full-screen intent、全屏 Activity、系统铃声和震动实现锁屏/后台触发；非 Android 或原生调度不可用时提供应用内前台全屏兜底。该能力仍受 Android 14+ full-screen notification、通知权限、精确闹钟权限、用户所选图片路径可读性和厂商后台策略影响。
- 工具箱/生活实用中的 `date_calculator` 日期计算器已从占位页升级为独立时间工作台：支持秒级起止时间选择、日历时间差、多单位加减、分数/百分比年数、目标日期进度、今年/本月/本周/本日/本小时动态剩余进度，以及按预期寿命展示的生命烛光。整数年/月按日历推进并处理月末夹取，小数年/月按对应自然年/月长度折算为秒；生命烛光默认 79 年，仅用于时间感知，不构成医学或寿命预测。
- 工具箱/生活实用新增 `world_clock` 世界时钟子模块：入口位于日期计算器下方，内置常见城市当前时间、本地时差、地区筛选、办公时段筛选和收藏时钟；时间计算在本地完成，不上传定位或请求网络。该模块使用内置城市表和基础夏令时规则，适合当前时间速查，不作为历史时区、政策变更或全部城市的权威时区数据库。

---

## 导航与交互

### 页面导航结构
```
首页 (HomePage)
├── 页面A (PageA)
│   └── 详情页A1 (DetailA1Page)
├── 页面B (PageB)
│   └── 详情页B1 (DetailB1Page)
└── 设置 (SettingsPage)
    ├── 主题设置 (ThemePage)
    └── 关于 (AboutPage)
```

### 移动端交互规范
- **抽屉(Drawer)**: 用于次要导航和设置
- **子页面**: 用于详情和表单填写
- **BottomSheet**: 用于快速选择和操作
- **SnackBar**: 用于操作反馈
- **底部导航栏**: 由 `AppShell` 统一渲染；默认常驻显示。全局设置可开启“下滑时自动隐藏底部导航”，开启后仅响应纵向滚动，下滑收起，上滑或点击屏幕后恢复显示，不再因滚动停止自动弹出，并同步调整迷你播放器与悬浮入口底部避让。

---

## 本地化

### 支持语言
| 语言 | 代码 | 状态 |
|------|------|------|
| 中文 | zh | 计划中 |
| English | en | 计划中 |

### 本地化范围
- 用户界面文本
- 日期时间格式
- 数字格式
- 货币格式 (如适用)

---

## API 规范

### API 基础配置
```dart
// 待填写
const apiBaseUrl = 'https://api.example.com';
const apiTimeout = Duration(seconds: 30);
```

### 接口版本策略
(待制定)

---

## 数据存储

### 本地存储策略
| 数据类型 | 存储方案 | 说明 |
|----------|----------|------|
| 用户配置 | SharedPreferences / Hive | 轻量键值 |
| 业务数据 | SQLite | 结构化数据 |
| 缓存数据 | Hive | 离线可用 |
| 媒体文件 | 本地文件系统 | 按需存储 |

### 单词本数据规范
- 新的标准化单词本统一采用 `wordbook.v1` 结构。
- 标准文档位于 `docs/wordbooks/WORDBOOK_STANDARD.md`。
- 2026-04-11 起，单词本主链路已按“核心字段 + 动态扩展字段”重建：`word + meaning` 为核心字段，其他字段按每条词的实际内容动态入库、展示和播放。
- 当前词本数据库承载已经支持 `wordbooks.schema_version / metadata_json` 以及 `words.entry_uid / primary_gloss / schema_version / source_payload_json / sort_index`，并配套 `word_fields / word_field_tags / word_field_media` 子表承载灵活扩展字段。
- 内置词本目录现已恢复，可从本地 asset 目录发现并建立懒加载占位；真实导入时支持从本地文件或字节流解析，后续可平滑切换到 S3/远端词本源。
- 大词本运行态继续采用“轻量列表 + 按需详情 + 懒加载 built-in”的策略，避免在移动端启动时无条件把整本词典一次性加载进 UI 内存。

---

## 性能目标

| 指标 | 目标值 |
|------|--------|
| 冷启动时间 | < 2秒 |
| 页面切换时间 | < 300ms |
| APK大小 (Release) | < 20MB |
| 内存占用 | < 150MB |

---

## 潜在风险

### 已识别风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| (待填写) | (待评估) | (待制定) |

---

## 待决策项

| 决策点 | 选项 | 状态 |
|--------|------|------|
| 状态管理方案 | flutter_bloc / riverpod / provider | 待选择 |
| 路由方案 | go_router / auto_route / Navigator | 待选择 |
| 离线策略 | 全量缓存 / 按需缓存 / 仅在线 | 待选择 |
| 推送通知 | (待选择第三方服务) | 待选择 |

---

## 更新记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-04-08 | v0.0.1 | 初始创建文档 |
| 2026-04-09 | v0.0.2 | 补充单词本标准化规范入口 |
| 2026-04-10 | v0.0.3 | 补充 `opencode` 多模型协作脚本与开发辅助工具说明 |
| 2026-04-10 | v0.0.4 | 新增 toolbox UI 设计规范基线文档入口 |
| 2026-04-10 | v0.0.5 | 标记旧单词本导入解析与导出恢复链路已临时下线，等待整体重写 |
| 2026-04-11 | v0.0.6 | 同步单词本与播放模块重建结果，恢复导入、导出恢复、built-in 目录和动态字段播放说明 |
| 2026-04-24 | v0.0.7 | 补充工具箱每日抉择五模块基础版说明与模块文档入口 |
| 2026-04-25 | v0.0.8 | 补充每日抉择“穿什么”模块的资料整理、天气默认建议与发布级数据能力说明 |
| 2026-04-25 | v0.0.9 | 补充每日抉择“穿什么”模块的结构化衣橱特征、87 条候选覆盖、详细穿搭指南与管理能力说明 |
| 2026-04-26 | v0.0.10 | 补充每日抉择“吃什么”模块的个人食谱集、集合内随机筛选、管理页分页性能与另存为个人食谱能力说明 |
| 2026-04-29 | v0.0.11 | 补充每日抉择“随机助手”子模块范围、随机方式和动画边界说明 |
| 2026-04-29 | v0.0.12 | 补充工具箱“人类测试”子模块范围、17 个趣味测试条目和本地即时结果边界 |
| 2026-05-18 | v0.0.13 | 补充环境迁移后的工具链适配说明，新增共享工具环境脚本、测试入口与 CMake 缓存迁移清理边界 |
| 2026-05-18 | v0.0.14 | 收口脚本中的个人绝对路径 fallback，数据生成默认改为环境变量、显式参数或项目内 `build/generated/` 路径 |
| 2026-05-19 | v0.0.15 | 补充工具箱“声源定位”子模块范围、手机移动确认路线、ODAS 可选高级证据和多声源/回响风险边界 |
| 2026-05-26 | v0.0.16 | 同步生活实用隐写工具的本地加密库、自由级联、文件载荷隐写和 keyfile 能力边界 |
| 2026-05-26 | v0.0.17 | 强化生活实用隐写安全边界：提升 scrypt、移除弱算法新加密、随机化图片 LSB、禁用伪音视频尾部写入 |
| 2026-05-26 | v0.0.18 | 收口生活实用隐写移动端页面：高级设置折叠、keyfile 开关化、多 keyfile 稳定组合和操作按钮层级区分 |
| 2026-05-26 | v0.0.19 | 同步生活实用隐写安全策略：新增 ChaCha20-Poly1305、隐写载荷错误尝试字节、尾部防篡改校验、定位密钥算法/强度和模块级还原锁定 |
| 2026-05-26 | v0.0.20 | 修复生活实用隐写错误限制执行：策略头支持错误口令读限、错误次数按文件持久记录、超过限制清理当前内存载体并按文件锁定 |
| 2026-05-26 | v0.0.21 | 修复隐写安全反馈：定位器 KDF 默认提强、策略头改为内容派生位置和 16 字节校验、公开策略头降级为本机提示边界、MAC 比较改为原始字节常量时间比较 |
| 2026-05-26 | v0.0.22 | 优化生活实用隐写还原性能：移除未发布旧格式兼容分支，并将文本/文件隐写生成与还原迁移到 compute isolate 减少 UI 主线程卡顿 |
| 2026-05-26 | v0.0.23 | 收口隐写还原界面：最大错误次数首次修改即提示销毁风险，还原失败仅显示错误次数，还原模式隐藏写入加密/签名设置并补充签名层性能边界 |
| 2026-05-26 | v0.0.24 | 新增隐写弱签名版：使用用户派生种子、内置域 key 和随机 nonce/padding 生成轻量 SHA-256/HMAC 标签，并提示其不提供第三方来源证明 |
| 2026-05-27 | v0.0.25 | 新增隐写重复写入保护与图片双层文本模式，并支持成功还原指定次数后清理当前文件副本中的隐藏载荷 |
| 2026-05-27 | v0.0.26 | 修复发布前安全审计项：release 签名不再复用 debug key、Android 禁用系统备份、加密/隐写输入增加资源上限、crypto envelope 移除公开明文与 keyfile 指纹，并将次数限制降级为本机当前文件最佳努力清理 |
| 2026-05-27 | v0.0.27 | 优化隐写文件选择内存路径：选择阶段先检查文件大小并使用 read stream/路径分段读取，同时移除未发布 crypto envelope v2/v3 兼容 |
| 2026-05-27 | v0.0.28 | 新增生活实用图片扩大模块：支持本地插值放大、目标像素扩大和补像素画布扩展 |
| 2026-05-27 | v0.0.29 | 新增生活实用历史年表/元素周期表动态可视化模块：接入共识历史节点、PubChem 118 元素数据、缩放筛选舞台和来源说明 |
| 2026-05-27 | v0.0.30 | 修复隐写交互状态与载体内存卸载：媒体类型独立于隐写文本/文件模式，自由级联贴近算法选择，写入前增加容量预检，解密导出或清理后卸载载体内存 |
| 2026-05-27 | v0.0.31 | 升级生活实用文本转换模块：收口火星文、文字隐藏、竖排排版、乱码候选修复、常用编码转换与码表查看，并按模式展示必要输入字段 |
| 2026-05-27 | v0.0.32 | 优化生活实用历史年表可读性：默认纵向故事轴、可选全景地图轴、沉浸全屏体验和公开资源背景 |
| 2026-05-27 | v0.0.33 | 扩展生活实用历史年表人类史节点体系，并将远程背景图改为超时缓存加载与本地渐变兜底 |
| 2026-05-27 | v0.0.34 | 补齐生活实用隐写音视频写入：WAV/PCM 随机样本 LSB、MP4/MOV 容器 free box、音视频容量预检和导出显式落盘 |
| 2026-05-27 | v0.0.34 | 实现生活实用带壳截图、全能单位换算与 BMI 计算器：补齐本地页面、双向换算/健康摘要与 PNG 导出链路 |
| 2026-05-27 | v0.0.34 | 新增生活实用城市薪资对比工具：支持双城市成本拆解、同薪资迁移估算与目标月薪换算 |
| 2026-05-27 | v0.0.34 | 扩展城市薪资对比工具个人校准层：支持预设基准倍数微调、健身/电影单项覆盖与自定义月支出 |
| 2026-05-27 | v0.0.34 | 扩展生活实用文本转换模块：并入拼音工具，新增数字书写、农历公历、干支八字、六十甲子、语言代码与简繁转换分组，并按移动端分组交互重整首屏结构 |
| 2026-05-27 | v0.0.35 | 扩展带壳截图机模与状态栏定制，修复窄屏预览偏移，并为全能单位换算新增力/密度/功率/热量/证件照/CSS 等分类与更多常用单位 |
| 2026-05-27 | v0.0.36 | 收口生活实用带壳截图主工作流：前置截图舞台，新增截图适配模式、大图导入闸门和预览/导出结构拆分 |
| 2026-05-27 | v0.0.37 | 优化生活实用带壳截图状态栏拟真：右侧状态组贴边限宽，电池按低电量、电量不足和充电状态切换图标与颜色 |
| 2026-05-27 | v0.0.38 | 修复生活实用模拟来电：移除触发前主舞台预览，倒计时改为手动输入与单位选择，Android 触发优先走闹钟级调度并持续响铃震动到用户关闭 |
| 2026-05-27 | v0.0.39 | 专业化增强生活实用 BMI 计算器：新增儿童青少年 BMI-for-age 百分位、性别/年龄口径、围度比例和 BMR/TDEE 估算 |
| 2026-05-27 | v0.0.40 | 专业化增强生活实用日期计算器：支持秒级时间差、多单位日期加减、动态周期进度、目标日期进度和生命烛光 |
| 2026-05-27 | v0.0.41 | 专业化实现生活实用房贷计算器：支持等额本息/等额本金、贷款金额/住房面积测算、首次还款日、已还期数、提前还款、费用和剩余计划明细 |
| 2026-05-27 | v0.0.42 | 完成生活实用图片转网页：生成单文件 HTML 图片预览页，并新增基于 Uguu 的临时图片上传分享 |
| 2026-05-27 | v0.0.43 | 完成生活实用 AI 面试训练模块：本地题型识别、答案框架、草稿体检、模拟追问和提示词草稿 |
| 2026-05-27 | v0.0.44 | 完成生活实用证件照生成模块：本地照片裁切、常见规格换算、简易换底色和 PNG/JPEG 导出 |
| 2026-05-27 | v0.0.45 | 补齐生活实用隐写文件双层加密与写入预览：文本/文件双层覆盖图片、WAV/PCM 和 MP4/MOV 载体，生成前展示容量与预计输出 |
| 2026-05-27 | v0.0.46 | 增强生活实用短链接与二维码：多短链服务、本地短码、重定向链路还原、QR/Data Matrix/Aztec/PDF417、本地样式和图片二维码化 |
| 2026-05-27 | v0.0.47 | 完成生活实用 Offer 选择助手：本地多 Offer 六维评分、风险提示、已婉拒对照、谈薪追平测算和参考工具链 |
| 2026-05-27 | v0.0.48 | 优化二维码图片二维码化：大图导入自动多档压缩到二维码友好体积，并展示原图/结果容量边界 |
| 2026-05-27 | v0.0.49 | 修复二维码超容量预览异常：渲染前强制容量校验，超长内容显示错误面板并禁用导出 |
| 2026-05-27 | v0.0.50 | 增强二维码 QArt 风格图片融合：支持图片取色、透明融合，并在图片 payload 超出当前 QR 容量时继续压缩 |
| 2026-05-28 | v0.0.51 | 优化二维码 QArt 艺术化：按导入图亮度、边缘和抖动生成半调模块，并保护 QR 功能区高对比 |
| 2026-05-28 | v0.0.52 | 优化二维码艺术图导入：大艺术图自动压缩为渲染副本，扩展 QR V1-V40 版本选择并增加弱纠错 V25 艺术预设 |
| 2026-05-28 | v0.0.53 | 新增工具箱加密安全模块，并将图片/音频/视频媒体隐写从生活实用迁入独立安全工作台 |
| 2026-05-28 | v0.0.54 | 补齐生活实用模拟来电接听/通话/挂断闭环，并支持来电与通话分别配置自定义背景图片样式 |
| 2026-05-28 | v0.0.55 | 优化生活实用模拟来电通话中效果：接听后以居中大面积脉冲动画舞台展示通话状态 |

---

## 备注
- 本文档需要根据项目实际情况持续更新
- 每次重大修改后应同步更新版本号
- 建议在每次迭代结束时回顾和更新本文档
