# 项目整体说明文档

## 文档版本
- **版本**: v0.0.26
- **更新日期**: 2026-05-27
- **状态**: 待完善

---

## 项目基本信息

| 项目属性 | 说明 |
|----------|------|
| 项目名称 | (待填写) |
| 项目类型 | Flutter 移动应用 |
| 目标平台 | iOS / Android |
| 最低版本 | (待填写) |
| 当前版本 | (待填写) |

---

## 项目背景与目标

### 项目背景
(待分析填写 - 请描述项目起源、核心需求来源)

### 核心目标
1. (待填写)
2. (待填写)
3. (待填写)

### 目标用户
- 主要用户群体: (待填写)
- 用户场景: (待填写)

---

## 技术栈

### 框架与语言
| 组件 | 版本 | 说明 |
|------|------|------|
| Flutter SDK | (待填写) | |
| Dart | (待填写) | |
| 状态管理 | (待选择) | flutter_bloc / riverpod / provider |
| 路由管理 | (待选择) | go_router / auto_route |
| 网络请求 | (待选择) | dio / http |

### 主要依赖
```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # 状态管理 (选择其一)
  flutter_bloc: ^8.1.3
  riverpod: ^2.4.9
  provider: ^6.1.1
  
  # 网络
  dio: ^5.4.0
  
  # 本地存储
  hive: ^2.2.3
  sqflite: ^2.3.0
  
  # 图片
  cached_network_image: ^3.3.0
  
  # UI组件
  flutter_slidable: ^3.0.1
```

---

## 项目结构

### 目录结构
```
lib/
├── main.dart                 # 应用入口
├── app.dart                  # 应用配置
├── core/                     # 核心模块
│   ├── constants/            # 常量定义
│   ├── theme/                # 主题配置
│   ├── utils/                # 工具类
│   └── extensions/           # 扩展方法
├── l10n/                     # 国际化资源
│   ├── app_en.arb
│   └── app_zh.arb
├── data/                     # 数据层
│   ├── models/               # 数据模型
│   ├── repositories/          # 数据仓库
│   ├── providers/            # 数据提供者
│   └── datasources/          # 数据源
│       ├── local/            # 本地数据源
│       └── remote/           # 远程数据源
├── domain/                   # 业务域
│   └── (按业务域组织)
├── presentation/             # 表现层
│   ├── pages/                # 页面
│   ├── widgets/              # 组件
│   └── blocs/                # 状态管理
└── services/                 # 服务层
    ├── navigation/           # 导航服务
    ├── storage/              # 存储服务
    └── network/              # 网络服务

scripts/
├── tooling-env.ps1                  # 共享工具链探测、PATH 注入与 CMake 缓存迁移清理
├── build.ps1                        # PowerShell 打包入口，支持 Windows/Android 等目标
├── dev-run.ps1                      # Windows 桌面快捷运行入口
├── test.ps1                         # Flutter 测试运行入口，统一 pub get、reporter 与缓存修复
├── verify-local-analysis.ps1         # 本地格式与 analyze 验证工具箱
├── opencode-minimax-m27.ps1          # 固定调用 MiniMax-M2.7 的命令模板
├── orchestrate-opencode-models.ps1   # 多模型 fan-out 调度脚本
└── opencode-model-profiles.json      # 外部模型 profile 映射配置
```

### 架构模式
- **状态管理**: (待选择: BLoC / Riverpod / Provider)
- **数据流**: (单向数据流)
- **依赖注入**: (待选择: get_it / riverpod / 手动)

### 开发辅助工具
- 当前仓库已内置本地验证脚本与 `opencode` 外部模型协作脚本，供开发期分析、方案草拟、并行对比和调度使用。
- `scripts/tooling-env.ps1` 集中处理 Flutter、Dart、CMake、Android SDK、NuGet 的解析与 PATH 注入，优先读取环境变量，再回退到项目内 `.fvm`、`.tooling` 和常见安装目录。
- `scripts/build.ps1`、`scripts/dev-run.ps1`、`scripts/test.ps1`、`scripts/verify-local-analysis.ps1` 会检查 `CMakeCache.txt` 是否残留旧工作区绝对路径；若发现从旧盘符复制带来的缓存错配，会清理对应生成目录后再继续。
- 运行时脚本不写死个人机器绝对路径；外部素材和生成数据默认使用环境变量、显式参数或项目内 `build/generated/` 输出目录。
- 外部模型输出仅作为辅助参考，不应直接替代对业务代码、架构边界和实际仓库上下文的本地判断。
- 多模型调度默认将结果写入工作区 `.tmp_model_runs/`，便于后续复查与归档。

### toolbox 设计文档
- `docs/toolbox_design/TOOLBOX_DESIGN_REVIEW.md`: 工具箱模块设计评审与问题清单。
- `docs/toolbox_design/TOOLBOX_ANIMATION_SPEC.md`: 工具箱动效规范与节奏建议。
- `docs/toolbox_design/TOOLBOX_UI_STYLE_GUIDE.md`: 当前生效的 toolbox UI 设计基线，用于约束后续“只动 UI、不动逻辑”的精修方向。
- `modules/toolbox/README.md`: 工具箱模块说明，包含每日抉择六模块基础版的数据边界、自定义存储和后续扩展路线。

---

## 功能模块

### 核心功能清单
| 模块 | 优先级 | 状态 | 备注 |
|------|--------|------|------|
| (待填写) | (待定) | 待开发 | |

### 功能特性矩阵
- 工具箱/每日抉择已从占位转盘升级为六模块基础版：
  - 吃什么、穿什么、去哪儿、干什么提供本地种子数据、随机选择、详情/指南和自定义管理；其中“吃什么”已支持个人食谱集、集合内筛选随机、内置菜个人调整与另存为个人食谱。
  - 其中“穿什么”已补充基于多本本地穿搭资料整理的规则库、`87` 条温度/场景搭配种子、天气体感驱动的默认档位建议、模块化穿搭指南，以及支持 `风格 / 版型 / 样式类型 / 面料 / 亮点` 的结构化衣橱管理。
  - 随机助手提供均匀、加权、联合分布多轮抽取，并配套大转盘、骰子和硬币动画；仅用于低风险、可回退的轻量选择。
  - 决策助手提供均匀随机、期望加权、因子评分和联合概率等透明计算工具。
  - 自定义项与吃什么个人食谱集暂使用每日抉择独立 JSON 文件存储，不侵入主数据库。
- 工具箱/人类测试新增独立 `toolbox.human_tests` 子模块：参考 Human Benchmark 类测试条目组织，提供反应测试、数字记忆、黑猩猩测试、打字测试、视觉记忆、瞄准测试、色觉测试、斯特鲁普、词汇记忆、序列记忆、运气测试、手速测试、时间感知测试、手眼协调测试、双手协调挑战、计算能力测试、动态视力测试和持续注意力测试；其中双手协调挑战已重做为左右独立脑裂小游戏集合，覆盖画图、弹球、楼梯、长按充能和同步窗口节奏，并补上手机默认 90 度横屏沉浸全屏、进入即开局、手眼协调同款全屏浮层、默认无限时长、轮数设置和窄屏左右并排；结果仅在本地页面即时展示，不进入主数据库或用户学习记录。
- 工具箱/声源定位新增独立 `toolbox.sound_locator` 子模块：默认使用手机自带麦克风和多位置移动确认，Flutter 端落地监听控制、空间指引舞台、移动采样点记录、下一步移动提示、多声源候选列表和回响/信噪比风险提示；双声道输入可补充 TDOA 水平方位估计，单声道输入也可通过多个位置的强度和质量差异逐步收敛声源区域，ODAS/阵列输出仅作为可选高级证据。
- 工具箱/生活实用隐写工具已扩展为本地加密与媒体隐写复合模块：支持文本/文件 payload 写入图片载体，内置 AES/Twofish/Camellia/ChaCha20-Poly1305 固定与自由级联、256/512/1024-bit 每层密钥材料、Whirlpool MAC/哈希、弱签名版 SHA-256/HMAC 轻量标签、SHA-256/RSA 与 ECDSA 高成本完整性附加校验、口令 + keyfile 混合派生和随机 keyfile 导出；弱签名版使用用户派生签名种子、内置域 key 和随机 nonce/padding 生成轻量标签，不生成公私钥，适合低性能移动设备附加校验，但不提供第三方来源证明；RSA/ECDSA 签名密钥当前仍由口令域确定性派生，公钥也写入同一 envelope，因此只作为同一口令域内的完整性附加校验，不作为独立来源证明；图片 LSB 使用密钥派生头与 CSPRNG 随机像素顺序，并支持定位密钥算法与强度设置，新写入定位器默认 4096 轮起步、strong/extreme 为 12000/24000 轮；新隐写保护块在数据段前记录一字节最大错误尝试次数，尾部加入前置数据轻量格式校验，图片同时写入基于图片内容派生位置的轻量策略头以便本机错误口令场景提示和清理兜底，但公开策略头、尾部校验和错误/成功次数限制均不作为抗恶意篡改、抗副本离线尝试或阅后即焚的安全边界，真正认证来自 crypto envelope 的 MAC/AEAD；由于模块尚未正式发布，新 crypto envelope 已升级并移除公开 `plainSha256` 与 `keyFileSha256`，keyfile 匹配交由 MAC/AEAD 失败处理；文本/文件隐写生成与还原在 UI 层使用 `compute` isolate 执行，降低图片解码、LSB 定位、KDF、scrypt 和 AEAD 对主线程的阻塞；触发篡改提示、达到载荷错误次数或成功还原次数上限时，仅对当前内存载体和可写源路径执行最佳努力清理；页面已将 keyfile 收口为开关式选项，支持多 keyfile 稳定排序组合，并强化高级设置折叠区辨识度，还原功能隐藏写入阶段才相关的加密算法、强度、签名和级联设置，仅保留口令/keyfile 与定位设置，并按当前文件统计 5 分钟内连续错误，达到 10 次后仅锁定该文件 5 分钟；RC4/SHA256 stream 仅保留旧载荷还原兼容，音视频新写入等待频域/帧级后端后再启用；该能力只处理本地媒体载荷，不进入主数据库或学习记录。
- 生活实用隐写写入流程已增加当前格式载体占用闸门：对可确认已由本模块写入的载体拒绝再次隐写，避免重复写入导致旧载荷残留或新旧定位冲突；如需多层内容，用户可显式开启图片双层文本模式，表层与深层写入不同 LSB 槽位并使用不同口令，表层口令只还原表层内容，深层口令只还原深层内容。高级保护块 `VSSG3` 同时支持“成功还原次数上限”，到达次数后优先清理当前可写文件副本或当前内存载体；该限制不约束已复制文件、外部备份或换客户端读取。

---

## 导航与交互

### 页面导航结构
```
首页 (HomePage)
├── 页面A (PageA)
│   └── 详情页A1 (DetailA1Page)
├── 页面B (PageB)
│   └── 详情页B1 (DetailB1Page)
└── 设置 (SettingsPage)
    ├── 主题设置 (ThemePage)
    └── 关于 (AboutPage)
```

### 移动端交互规范
- **抽屉(Drawer)**: 用于次要导航和设置
- **子页面**: 用于详情和表单填写
- **BottomSheet**: 用于快速选择和操作
- **SnackBar**: 用于操作反馈

---

## 本地化

### 支持语言
| 语言 | 代码 | 状态 |
|------|------|------|
| 中文 | zh | 计划中 |
| English | en | 计划中 |

### 本地化范围
- 用户界面文本
- 日期时间格式
- 数字格式
- 货币格式 (如适用)

---

## API 规范

### API 基础配置
```dart
// 待填写
const apiBaseUrl = 'https://api.example.com';
const apiTimeout = Duration(seconds: 30);
```

### 接口版本策略
(待制定)

---

## 数据存储

### 本地存储策略
| 数据类型 | 存储方案 | 说明 |
|----------|----------|------|
| 用户配置 | SharedPreferences / Hive | 轻量键值 |
| 业务数据 | SQLite | 结构化数据 |
| 缓存数据 | Hive | 离线可用 |
| 媒体文件 | 本地文件系统 | 按需存储 |

### 单词本数据规范
- 新的标准化单词本统一采用 `wordbook.v1` 结构。
- 标准文档位于 `docs/wordbooks/WORDBOOK_STANDARD.md`。
- 2026-04-11 起，单词本主链路已按“核心字段 + 动态扩展字段”重建：`word + meaning` 为核心字段，其他字段按每条词的实际内容动态入库、展示和播放。
- 当前词本数据库承载已经支持 `wordbooks.schema_version / metadata_json` 以及 `words.entry_uid / primary_gloss / schema_version / source_payload_json / sort_index`，并配套 `word_fields / word_field_tags / word_field_media` 子表承载灵活扩展字段。
- 内置词本目录现已恢复，可从本地 asset 目录发现并建立懒加载占位；真实导入时支持从本地文件或字节流解析，后续可平滑切换到 S3/远端词本源。
- 大词本运行态继续采用“轻量列表 + 按需详情 + 懒加载 built-in”的策略，避免在移动端启动时无条件把整本词典一次性加载进 UI 内存。

---

## 性能目标

| 指标 | 目标值 |
|------|--------|
| 冷启动时间 | < 2秒 |
| 页面切换时间 | < 300ms |
| APK大小 (Release) | < 20MB |
| 内存占用 | < 150MB |

---

## 潜在风险

### 已识别风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| (待填写) | (待评估) | (待制定) |

---

## 待决策项

| 决策点 | 选项 | 状态 |
|--------|------|------|
| 状态管理方案 | flutter_bloc / riverpod / provider | 待选择 |
| 路由方案 | go_router / auto_route / Navigator | 待选择 |
| 离线策略 | 全量缓存 / 按需缓存 / 仅在线 | 待选择 |
| 推送通知 | (待选择第三方服务) | 待选择 |

---

## 更新记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-04-08 | v0.0.1 | 初始创建文档 |
| 2026-04-09 | v0.0.2 | 补充单词本标准化规范入口 |
| 2026-04-10 | v0.0.3 | 补充 `opencode` 多模型协作脚本与开发辅助工具说明 |
| 2026-04-10 | v0.0.4 | 新增 toolbox UI 设计规范基线文档入口 |
| 2026-04-10 | v0.0.5 | 标记旧单词本导入解析与导出恢复链路已临时下线，等待整体重写 |
| 2026-04-11 | v0.0.6 | 同步单词本与播放模块重建结果，恢复导入、导出恢复、built-in 目录和动态字段播放说明 |
| 2026-04-24 | v0.0.7 | 补充工具箱每日抉择五模块基础版说明与模块文档入口 |
| 2026-04-25 | v0.0.8 | 补充每日抉择“穿什么”模块的资料整理、天气默认建议与发布级数据能力说明 |
| 2026-04-25 | v0.0.9 | 补充每日抉择“穿什么”模块的结构化衣橱特征、87 条候选覆盖、详细穿搭指南与管理能力说明 |
| 2026-04-26 | v0.0.10 | 补充每日抉择“吃什么”模块的个人食谱集、集合内随机筛选、管理页分页性能与另存为个人食谱能力说明 |
| 2026-04-29 | v0.0.11 | 补充每日抉择“随机助手”子模块范围、随机方式和动画边界说明 |
| 2026-04-29 | v0.0.12 | 补充工具箱“人类测试”子模块范围、17 个趣味测试条目和本地即时结果边界 |
| 2026-05-18 | v0.0.13 | 补充环境迁移后的工具链适配说明，新增共享工具环境脚本、测试入口与 CMake 缓存迁移清理边界 |
| 2026-05-18 | v0.0.14 | 收口脚本中的个人绝对路径 fallback，数据生成默认改为环境变量、显式参数或项目内 `build/generated/` 路径 |
| 2026-05-19 | v0.0.15 | 补充工具箱“声源定位”子模块范围、手机移动确认路线、ODAS 可选高级证据和多声源/回响风险边界 |
| 2026-05-26 | v0.0.16 | 同步生活实用隐写工具的本地加密库、自由级联、文件载荷隐写和 keyfile 能力边界 |
| 2026-05-26 | v0.0.17 | 强化生活实用隐写安全边界：提升 scrypt、移除弱算法新加密、随机化图片 LSB、禁用伪音视频尾部写入 |
| 2026-05-26 | v0.0.18 | 收口生活实用隐写移动端页面：高级设置折叠、keyfile 开关化、多 keyfile 稳定组合和操作按钮层级区分 |
| 2026-05-26 | v0.0.19 | 同步生活实用隐写安全策略：新增 ChaCha20-Poly1305、隐写载荷错误尝试字节、尾部防篡改校验、定位密钥算法/强度和模块级还原锁定 |
| 2026-05-26 | v0.0.20 | 修复生活实用隐写错误限制执行：策略头支持错误口令读限、错误次数按文件持久记录、超过限制清理当前内存载体并按文件锁定 |
| 2026-05-26 | v0.0.21 | 修复隐写安全反馈：定位器 KDF 默认提强、策略头改为内容派生位置和 16 字节校验、公开策略头降级为本机提示边界、MAC 比较改为原始字节常量时间比较 |
| 2026-05-26 | v0.0.22 | 优化生活实用隐写还原性能：移除未发布旧格式兼容分支，并将文本/文件隐写生成与还原迁移到 compute isolate 减少 UI 主线程卡顿 |
| 2026-05-26 | v0.0.23 | 收口隐写还原界面：最大错误次数首次修改即提示销毁风险，还原失败仅显示错误次数，还原模式隐藏写入加密/签名设置并补充签名层性能边界 |
| 2026-05-26 | v0.0.24 | 新增隐写弱签名版：使用用户派生种子、内置域 key 和随机 nonce/padding 生成轻量 SHA-256/HMAC 标签，并提示其不提供第三方来源证明 |
| 2026-05-27 | v0.0.25 | 新增隐写重复写入保护与图片双层文本模式，并支持成功还原指定次数后清理当前文件副本中的隐藏载荷 |
| 2026-05-27 | v0.0.26 | 修复发布前安全审计项：release 签名不再复用 debug key、Android 禁用系统备份、加密/隐写输入增加资源上限、crypto envelope 移除公开明文与 keyfile 指纹，并将次数限制降级为本机当前文件最佳努力清理 |

---

## 备注
- 本文档需要根据项目实际情况持续更新
- 每次重大修改后应同步更新版本号
- 建议在每次迭代结束时回顾和更新本文档
